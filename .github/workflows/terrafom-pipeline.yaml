name: Infrastructure provisioning pipeline 
on: 
  workflow_dispatch:
jobs: 
  DEV: 
    runs-on: X64
    uses: actions/checkout@v4
    with: 
      sparse-chekout: |
        ansible
        terraform
      
    steps: 
      - run: echo test
        name: run terraform 
      - run: |
          ls -la
          cd terraform
          ls -la
          terraform init  
     